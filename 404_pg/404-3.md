<div class="brutal-wrapper">
    <div class="brutal-content">
        <div class="brutal-number">ERROR 404</div>
        <div class="brutal-line"></div>
        <div class="brutal-msg">THIS PAGE DOES NOT EXIST</div>
        <div class="brutal-line"></div>
        <div class="brutal-desc">
            <span>THE REQUESTED RESOURCE COULD NOT BE FOUND ON THIS SERVER.</span><br>
            <span>POSSIBLE CAUSES: BROKEN LINK, TYPO IN URL, OR THE PAGE WAS MOVED.</span>
        </div>
        <div class="brutal-line"></div>
        <div class="brutal-actions">
            <a href="#/" class="brutal-btn">[ HOME ]</a>
            <a href="#/doc/other/list.md" class="brutal-btn">[ INDEX ]</a>
            <a href="#/doc/other/about_me.md" class="brutal-btn">[ ABOUT ]</a>
        </div>
    </div>
</div>

<style>
.brutal-wrapper {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: calc(100vh - 120px);
    padding: 2rem;
    font-family: 'Impact', 'Arial Black', 'Helvetica Neue', sans-serif;
}

.brutal-content {
    max-width: 700px;
    width: 100%;
    text-align: left;
    animation: brutalFade 0.5s ease-out;
}

@keyframes brutalFade {
    from { opacity: 0; transform: scale(0.98); }
    to { opacity: 1; transform: scale(1); }
}

.brutal-number {
    font-size: clamp(4rem, 12vw, 7rem);
    font-weight: 900;
    color: var(--text-color, #000);
    letter-spacing: -3px;
    line-height: 1;
    margin-bottom: 0.3rem;
}

.brutal-line {
    height: 6px;
    background: var(--text-color, #000);
    width: 100%;
    margin: 0.8rem 0;
}

.brutal-msg {
    font-size: clamp(1.2rem, 3vw, 1.6rem);
    font-weight: 700;
    color: var(--text-color, #000);
    letter-spacing: 2px;
    margin: 1rem 0;
}

.brutal-desc {
    font-size: clamp(0.75rem, 1.8vw, 0.85rem);
    font-weight: 400;
    color: var(--text-color, #555);
    line-height: 1.8;
    margin: 1rem 0;
    font-family: 'Courier New', Courier, monospace;
}

.brutal-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    margin: 1.5rem 0;
}

.brutal-btn {
    display: inline-block;
    padding: 0.8rem 2rem;
    background: var(--text-color, #000);
    color: var(--background, #fff);
    text-decoration: none;
    font-size: 0.9rem;
    font-weight: 700;
    letter-spacing: 1px;
    transition: none;
}

.brutal-btn:hover {
    background: transparent;
    color: var(--text-color, #000);
    outline: 3px solid var(--text-color, #000);
}

@media (prefers-color-scheme: dark) {
    .brutal-btn {
        background: #fff;
        color: #1a1a1a;
    }
    .brutal-btn:hover {
        background: transparent;
        color: #fff;
        outline-color: #fff;
    }
    .brutal-desc { color: #999; }
}

@media (max-width: 600px) {
    .brutal-wrapper { padding: 1rem; }
    .brutal-actions { flex-direction: column; }
    .brutal-btn { text-align: center; }
}
</style>
