<div class="pixel-wrapper">
    <div class="pixel-content">
        <div class="pixel-screen">
            <div class="pixel-title">404</div>
            <div class="pixel-subtitle">PAGE NOT FOUND</div>
            <div class="pixel-heart">
                <span class="heart heart-a"></span>
                <span class="heart heart-b"></span>
                <span class="heart heart-c"></span>
                <span class="heart heart-d"></span>
                <span class="heart heart-e"></span>
                <span class="heart heart-f"></span>
                <span class="heart heart-g"></span>
                <span class="heart heart-h"></span>
            </div>
            <div class="pixel-text">Oops! You found a secret area...</div>
            <div class="pixel-text">...but there's nothing here!</div>
            <div class="pixel-actions">
                <a href="#/" class="pixel-btn">&#9664; START</a>
                <a href="#/doc/other/list.md" class="pixel-btn">&#9664; ITEMS</a>
                <a href="#/doc/other/about_me.md" class="pixel-btn">&#9664; STATUS</a>
            </div>
        </div>
    </div>
</div>

<style>
@import url('https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap');

.pixel-wrapper {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: calc(100vh - 120px);
    padding: 2rem;
    font-family: 'Press Start 2P', monospace;
    image-rendering: pixelated;
}

.pixel-content {
    animation: pixelFloat 2s ease-in-out infinite;
}

@keyframes pixelFloat {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-8px); }
}

.pixel-screen {
    background: var(--border-color, #2a2a3a);
    border: 4px solid var(--text-color, #333);
    padding: 2rem;
    max-width: 500px;
    width: 100%;
    text-align: center;
    box-shadow: 8px 8px 0 var(--text-color, #333);
    position: relative;
}

.pixel-title {
    font-size: clamp(3rem, 10vw, 5rem);
    color: #e74c3c;
    text-shadow: 4px 4px 0 #c0392b;
    margin-bottom: 0.5rem;
    letter-spacing: 5px;
}

.pixel-subtitle {
    font-size: clamp(0.5rem, 1.5vw, 0.65rem);
    color: var(--text-color, #555);
    margin-bottom: 1.5rem;
    letter-spacing: 3px;
}

/* 8-bit heart using box-shadow */
.pixel-heart {
    display: flex;
    justify-content: center;
    gap: 2px;
    margin-bottom: 1.5rem;
}

.heart {
    display: inline-block;
    width: 8px;
    height: 8px;
    background: #e74c3c;
}

.heart-a { box-shadow: 8px 0 #e74c3c, -8px 0 #e74c3c; }
.heart-b { box-shadow: 8px 0 #e74c3c, -8px 0 #e74c3c, 0 -8px #e74c3c; }
.heart-c { box-shadow: 0 -8px #e74c3c; }
.heart-d { box-shadow: 0 -8px #e74c3c; }
.heart-e { box-shadow: 8px 0 #e74c3c, -8px 0 #e74c3c, 0 -8px #e74c3c; }
.heart-f { box-shadow: 8px 0 #e74c3c, -8px 0 #e74c3c, 0 -8px #e74c3c; }
.heart-g { box-shadow: 0 -8px #e74c3c; }
.heart-h { box-shadow: 0 -8px #e74c3c; }

.pixel-text {
    font-size: clamp(0.4rem, 1.3vw, 0.55rem);
    color: var(--text-color, #777);
    line-height: 2;
}

.pixel-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
    justify-content: center;
    margin-top: 1.5rem;
}

.pixel-btn {
    font-family: 'Press Start 2P', monospace;
    font-size: clamp(0.35rem, 1.2vw, 0.5rem);
    color: var(--text-color, #333);
    text-decoration: none;
    padding: 0.6rem 1rem;
    border: 2px solid var(--text-color, #333);
    transition: all 0.1s;
    letter-spacing: 1px;
}

.pixel-btn:hover {
    background: var(--text-color, #333);
    color: var(--background, #fff);
}

@media (prefers-color-scheme: dark) {
    .pixel-screen { border-color: #555; box-shadow: 8px 8px 0 #555; }
    .pixel-btn { border-color: #aaa; color: #ccc; }
    .pixel-btn:hover { background: #ccc; color: #1a1a1a; }
}

@media (max-width: 600px) {
    .pixel-wrapper { padding: 1rem; }
    .pixel-screen { padding: 1.2rem; }
    .pixel-actions { flex-direction: column; align-items: center; }
}
</style>
